<%
function compareT(a, b)
{
if (a.title < b.title)
  return -1;
if (a.title > b.title)
return 1;
}
function compareA(a, b)
{
  if (a.author < b.author)
    {return -1;}
  if (a.author > b.author)
    {return 1; }
  return 0;
}
function compareD(a, b)
      {
        if (a.id < b.id)
          {return -1;}
        if (a.id > b.id)
          {return 1; }
        return 0;
      }
%>

